/**
 * Move resolver — given attacker, defender, and a move, produce the
 * post-effect deltas. Deferred to Phase 2 when we server-authoritatively
 * resolve moves. Phase 1 resolves moves on the client.
 */
export function resolveMove(/* attacker, defender, move */) {
  return null;
}
