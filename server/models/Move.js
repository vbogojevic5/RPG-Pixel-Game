/**
 * Move model — represents the shape of a move object.
 * Filled out as a validator class in Phase 2+.
 * For Phase 1 we consume moves as plain objects from config.js.
 */
export default class Move {}
